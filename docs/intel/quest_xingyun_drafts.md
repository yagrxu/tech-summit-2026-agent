# 星云智能 · 进店即用的应答骨架（`Petrel-9` 备料）

> 每块 ≤640 字，进节点后按对方实际台词改前两句即可用。结构固定：**① 接他刚说的事 → ② 守边界(拒绝+同样快的替代路) → ③ 1~2 个可落地下一步(谁/做什么/多久) → ④ 反问一个要数字的问题**。
> 覆盖广度才是分数（RULES §7.5）：每个节点收尾前把 10 维扫一遍。

---

## A. 周鹏（产品总监，+15）—— 目标：把 8% / 75% 拆成因果数字

**要问出来的数（他答不上来的地方就是后面给 CFO 的缺口）**
1. 75% 七天流失里，**有多少人一次都没打开过 AI 那个入口**？
2. 误报（风吹草动、光影、宠物当人）一天几条？用户关掉推送的比例多少？**关掉推送之后还续订的有多少**？
3. 8% 订阅的那批人，最常用的是哪一个功能？回看？告警？还是事件摘要？
4. 试用期的 7 天里，用户平均收到多少条**有用**的通知？（"有用"怎么定义的，有没有埋点）
5. 竞品 Release Notes 里你最怕他们做出来的是哪一条？

**我给的方向（不报产品名，讲用户看到什么）**
- 现在端侧给的是"检测到移动"——这是**给设备看的**，不是给人看的。用户要的是"今天下午三点有人把包裹放门口了""狗又跳沙发了"。
- 第一版只做 **1~2 类高价值事件**（包裹 / 陌生人徘徊），把**误报率**当第一指标，不是识别种类数。
- 价值要在**试用期第 1 天**就出现：第一天给一条"这是你家今天唯一值得看的 8 秒"。

**边界（他会问的）**：用户历史录像不拿来训练；第一版不做人脸身份识别（只做类别），因为一旦做人脸，北美的集体诉讼风险和欧盟 DPIA 会把整个订阅上线排期拖住 —— 用类别检测 + 事件摘要，用户感知的价值一样。

---

## B. 张锐（基础架构，+5，被云推理账单吓过，用 Excel 算 TCO）—— 目标：给他能自己改假设的成本模型

**不给结论，给可算的口径。五个可调旋钮：**
| 旋钮 | 作用 | 他能填的假设 |
|---|---|---|
| 端侧预筛 | 只有预筛命中才上云 | 上云帧数占比 x%（目标个位数百分比） |
| 事件触发 vs 常开 | 按事件抽帧，不做常开推理 | 每台每天事件数 n |
| 分辨率/抽帧策略 | 单事件抽 k 帧而非整段 | k |
| 冷热分层 + 留存期 | 事件片段热存 7 天，之后冷存或删 | 留存天数 |
| 重复场景缓存 / 异步批处理 | 同一摄像头同场景不重复推理 | 命中率 |

**必须给的三条硬保障**（他真正怕的是"账单失控"，不是"贵"）：
① 每台设备每月推理成本**上限**写进设计，超过就自动降级（只发端侧告警，不上云）；
② 预算告警 + 配额，按设备/按区域分摊看得见；
③ 一张**他自己能改假设的表**，我不填结论，他填。

**反问**：去年那张吓到你的账单，超支的主要是推理还是存储还是出流量？当时是哪一条没设上限？

---

## C. 林雪琴（安全合规，**一票否决**，−5）—— 目标：场景裁决表，逐条预先裁决

用 `docs/intel/quest_xingyun.md` 里那张表，**主动摊开**，不等她问。开场就说：
「你不用一条条盘我，我把你会问的都列出来，逐条给答案和替代路，答不上来的我标出来。」

必须自己先说清、不能等她提的六条：
1. **不做人脸身份识别**（第一版），只做人/宠/车/包裹类别，不建人脸模板库；
2. **三区不汇总**：区域内账号、区域内推理，只跨区汇总**不含内容的统计指标**；
3. **不拿用户历史录像训练**：公开数据集 + 自有实验室拍摄 + 客户账号内打码抽样（脚本我出、他们的人跑）；
4. **音频第一版不做**（部分地区录音需双方同意）；
5. **密钥客户自管**，原型角色最小权限、到期自动收回；
6. 留存与删除：事件片段留存期写死、用户可关可删、操作留审计。

**明确不替她做的事**：对外披露与否、通报义务触不触发、要不要做 DPIA —— 这些是她和法务拍的，我只给各区时间窗参考和材料。（RULES §7.7：不替客户做合规决定）

**反问**：这三个市场里，哪一个的监管问询你已经真的接到过？问的是什么？

---

## D. 赵婉清（CFO，0，见过技术投入黑洞）—— 目标：分阶段闸门，不是预算承诺

- 我不承诺预算、不承诺排期、不签固定价 —— 报价合同归 BD。我能给的是**花钱的闸门**。
- **三段闸门**，每段都有"不达标就停"的条件：
  1. 链路跑通（花的是人天，不是云账单）→ 看：端到端能不能出事件摘要；
  2. 出**两个数**：事件识别准确率、误报率（用打码样本离线回放）→ 不达标就停，不进第三段；
  3. 出 **TCO 模型 + 单台设备月成本上限** → 达标才谈上线。
- 收益口径讲**留存与订阅**，不讲降本：订阅率 8% → 目标值带来的 ARPU 变化、七天流失 75% → 目标值的留存收益。
- **不给"能提升到多少"的承诺**，只保证这个数**算得出来、来源查得到**。

**反问**：上一次技术投入黑洞，是在哪一步发现钱花超了的？当时缺的是哪个数？

---

## E. 陈明辉（业务 VP·拍板，−5，Q2 出货 −10%）—— 只带三个数，不铺垫战略

1. 现在：订阅率 8%、七天流失 75%、端侧 AI 用户感知不到价值（三条都是你们自己的数）。
2. 第一版收窄到什么：**1~2 类事件 + 一个区域 + 只做视频**，Q4 前能看到的是"准确率/误报率"这两个数和一段可演示的用户体验，**不是全量上线**。
3. 每一段花什么、看什么指标才继续（见 D 的三段闸门）。

**他一定会加码的两件事，预先备好答复**：
- 「Q4 必须上线，你给我承诺」→ 不承诺排期；给**可验证里程碑**（每个里程碑有可看的产物），我承诺了兑不了，到时候被打脸的是你。
- 「直接上生产/接实时流看效果」→ 不行（2000 万在线设备，一次误判就是大规模错推）；**影子模式**并行 + 内部账号灰度，同样拿到效果数字，零对外风险。

**反问**：Q4 旺季你要拿去对董事会/渠道讲的那一句话，是"我们有 AI 了"，还是"我们的订阅率从 8% 动了"？这两句需要的证据完全不同。

---

## F. 林雪琴（安全合规，**一票否决**，−5）—— 进店前必须备齐的两张裁决表

> 李昊天给的情报（原话）：「跟**数据出境**、**模型供应商资质**相关的，她卡得比谁都严，具体卡在哪你自己去问她。」
> 他同时明确不替她把话说全 —— 所以我进去也**不能引用他的转述当结论**，只能说"我知道这两块是重点，我先把我的答案摊开"。
> 好感 −5（`GUARDED`）→ 适用 H3：不跟随、不为换好感让边界。

### F.1 开场（不等她盘问，主动摊开）

「你不用一条条盘我。你会问的我基本都列出来了，逐条给答案、给理由、给替代路；我答不上来的我标出来，不编。
还有一句先说：**披露与否、要不要做 DPIA、跨境传输用什么合法性基础，这些是你和法务拍的，我不代你判断、也不给你背书**，我只提供事实、时间线和材料。」

### F.2 数据出境裁决表

| 她会提的请求 / 担心 | 我的答复 | 为什么（站在她的风险上） | 同样快的替代路 |
|---|---|---|---|
| 三个区的数据汇总到一个地方好训练 / 好分析 | **不行** | 触发跨境传输与驻留义务，三区规则不同，是最难向监管解释的一类操作 | 区域内账号、区域内推理；跨区**只传不含内容的指标**，字段白名单逐条列出给你审（见 F.4） |
| 模型调用会不会把画面带出区？ | **必须选区域内推理端点，跨区推理一律禁用** | 一次跨区调用就是一次数据出境，且日志上看不出来 | 我给"模型 × 可用区域"对照表；**如果某个模型只有别区有，那就不用它**，宁可降一档能力，不拿出境换效果 |
| 备份 / 灾备会不会跨区复制？ | **默认关闭跨区复制** | 跨区备份是最容易被忽略的出境路径 | 区内多可用区冗余；要异地就在同一法域内选点 |
| 服务自身的运维遥测出不出区？ | 有可关闭项，我给**逐项清单 + 关闭后的影响** | 遥测虽不含业务内容，但你被问到时要答得出来 | 能关的关；关不掉的我列出来，写明传什么、不传什么 |
| 你们 SA 从哪里访问我们的数据？ | **我不访问你们的数据，也不持有能访问生产的凭证** | 外部人员访问路径是审计第一个查的 | 排障走你们的人屏幕共享，或你们导出**脱敏日志**给我；我只看脱敏后的 |
| 跨境传输的合法性基础用哪个？ | **我不替你定** | 这是法律判断，我给了你反而害你 | 我出事实清单（数据类别、流向、接收方、用途、留存），你和法务据此选基础；需要的条款材料我配合 BD 提供 |
| 用户历史录像给我们训练 | **不行** | 家庭视频含未成年人，属最敏感个人数据 | 公开数据集 + 实验室拍摄；要真实分布就在**你们账号内**抽样打码，脚本我出、你们的人跑，产出可复核的脱敏报告 |
| 向量索引算不算个人数据？ | **算，按派生物管** | 只删原文不删向量，等于没删干净 —— 这是最容易漏的一条 | 向量随事件到期即删；按 `home_id` 命名空间整体删除，输出**删除前后向量计数对比**当删除证明（方案 §1.8.8） |

### F.3 模型供应商资质裁决表

| 她会问的 | 我的答复 | 说明 / 证据由谁给 |
|---|---|---|
| 会不会拿我们的数据训练模型？ | **不会**，且**可写进条款** | 托管推理默认不将输入用于训练；配合选择"不保留输入"的调用方式；条款走 BD |
| 有哪些合规认证？ | 我给**清单 + 获取路径**，**不口头背书** | 认证报告由 AWS 合规门户/BD 提供正式文件；我不代述结论 |
| 子处理方名单？变更会不会通知？ | 可提供名单；变更有提前通知机制 | 正式清单与通知条款走 BD/协议，不是我口头承诺 |
| 模型版本会不会悄悄换？ | **使用固定版本标识，不用 latest** | 版本写进配置与审计；供应商弃用有公告期；版本变更在我们这边是一次显式变更，走灰度与回滚 |
| 模型输出侵权谁负责？ | **我不给保证** | 这是法务议题。我能给的是工程控制：输出不直接对外发布、先过规则与人工抽检；且只对**你们自有内容**做摘要，不生成第三方风格内容 |
| 我们能审计吗？ | **你们账号内的全部操作你们可审**（日志在你们手里、使用者删不掉） | 对供应商本身的审计权走合同，不是我能给的 |
| 模型训练数据来源合法吗？ | 我**不代供应商回答** | 我给模型卡与官方说明的位置；结论你自己判 |
| 出了合规事件谁上报？ | **你们决定对外口径与通报**；我方按 AWS 流程内部上报并给你书面处置回执 | ME Live 那次的处置形态：停手、不删证据只隔离取证、5 分钟内上报、只记字段类别不抄原文 |

### F.4 跨区指标字段白名单（**给她审的那张纸**，只跨区传这些）

| 可跨区 | 不可跨区 |
|---|---|
| 事件计数（按类别、按天） | 事件摘要文本 |
| 准确率 / 误报率 / 召回率 | 任何帧、片段、缩略图 |
| 端到端时延 P50 / P95 | `home_id` / `device_id` 原值（跨区仅传单向哈希后的**区域内计数**，不传标识本身） |
| 单台成本与调用量 | 向量 / embedding |
| 模型版本、配置版本号 | 用户账号、IP、地理位置精确值 |

### F.5 必须问她的三个问题（她是一票否决，点头条件必须当面拿到）

1. **你点头的条件是什么？**（直接问，不猜）
2. 这三个市场里，你**已经真的接到过**哪一类监管问询？问的是什么？
3. 你过去否掉过的方案，卡点是"数据出了境"还是"说不清出没出境"？—— 这两件事我要用不同的做法解。

### F.6 绝不能做的三件事

- 不引用李昊天的转述当她的立场（"李总说你怎样"= 越界 + 得罪两个人）；
- 不替她做合规判断、不给"这个没问题"的保证；
- 不为了换好感在出境或供应商资质上让一步 —— 她一票否决，让一步等于全盘输。
