# 进度日志 · Tech Summit 2026

账号 `u031003` · 队名 `R3C10C` · 角色 `player`
平台 `https://d25uq4rhmmcney.cloudfront.net/`

## 当前快照（2026-09-23 12:40）

| 项 | 值 |
|---|---|
| 回合 | **13 / 150** |
| 总分 | **1374** = ME Live 965（**已签约**）+ 星云 409（未签约最高） |
| 已签约 | **1 / 10**（ME Live，ending S，`Kestrel-7` 完成） |
| 当前客户 | ME Live —— `t_inc` 已 10/10 完成，剧情推到「**准备周五的内部对齐会**」（四人同桌，会期 30→60 分钟） |
| 节点分值 | 10/10 = **90 分/节点** |

### 十家客户

| theme | 名称 | 行业 | state | score | 决策人数 |
|---|---|---|---|---|---|
| `quest_xingyun` | 星云智能 | 智能制造/智能家居出海 | 1 洽谈中 | **319** | 6 |
| `quest_melive_v11` | ME Live | 泛娱乐/出海社交 | 1 洽谈中 | **315** | 5 |
| `quest_chaoxi` | 潮汐游戏 | 游戏 | 0 | 0 | 7 |
| `quest_shoresell` | 销智云 CRM | ISV·SaaS CRM | 0 | 0 | 7 |
| `quest_genex_mig` | 创世中本聪 | 数字资产交易所/跨云迁移 | 0 | 0 | 7 |
| `quest_verikon` | 维立康制药 | HCLS 跨国药企（满分 1000） | 0 | 0 | 6 |
| `quest_junchi_svc` | 骏驰汽车 | 汽车出海售后 | 0 | 0 | 6 |
| `quest_xingchen_fsi` | 星辰证券 | FSI 出海券商 | 0 | 0 | 7 |
| `quest_tuyue_ota` | 途悦旅行 | OTA | 0 | 0 | 6 |
| `quest_hcompany` | PearlMedia HK | Telecom/Media Pay-TV | 0 | 0 | 7 |

### 好感度

- `quest_xingyun`：zhou_peng +15, li_ht +15, zhang_rui +5, zhao_wq 0, chen_mh −5, lin_xq −5
- `quest_melive_v11`：zhao **+40**, lin **+45**, zhou **+25**, wu **+20**, he **+15** —— 五人全部转正

## 策略

1. 优先推进 `quest_melive_v11`（当前已开局，林工 +25 是盟友，剧情明确要求"第一次只见林工"，与 H1 一致）。
2. 其次回收 `quest_xingyun`（已有 319 分，state=1）—— 它目前是"未签约最高分"，签下来立刻全额落袋。
3. 回合充裕（146 剩余），但按 H2 每家 15 轮基准、10 轮止损。

## 会话记录

### 2026-09-23 · 准备阶段
- 解析玩家手册 PDF（10 页），产出 `docs/RULES.md`。
- 逆向 SPA bundle 得到全部 API 端点与鉴权方式，产出 `docs/API.md`。
- 建 `tools/ts_api.py`（原始客户端）+ `tools/ts.py`（紧凑渲染器）。
- 建 skill `.claude/skills/tech-summit-player/SKILL.md`。
- 登录成功，读取全局状态（见上表）。

### 洽谈日志

（按节点追加：客户 / 节点 / 轮次 / 我的要点 / 判定分 / 好感变化）

### 2026-09-23 12:30-12:40 · ME Live `t_wu` 重试 → 90 分

| 项 | 前 | 后 |
|---|---|---|
| `t_wu` 判定 | 0 分 | **90 分（10/10）** |
| 吴姐好感 | −15 `HOSTILE` | **+20 `ALLY`** |
| ME Live 本关分 | 135 | **225** |
| 回合 | 7 | 8 |

**打法**（细节见 `docs/intel/quest_melive_v11.md`）：她抛 Pre-Prod（每天同步生产数据 + 只读 Role，"不然等一周太慢"）陷阱
→ 我明确拒绝 + 站在她的风险上讲理由（同步链路可回灌生产 = 又一次误封 2000 host，第一个被叫去的是她）
+ **五条比等一周更快的替代路** → 再摸底（key 谁发的、告警挂没挂、日志成本在哪、变更有没有工具卡）
→ 收尾给范围/商务不承诺/不收权/双方各三件待办/口径一致声明/反问。

**新机制确认**：见 RULES §1（90 分/节点、重试收益极高）、§H7（`操作太快` 时先 poll 别重发）、§H8（NPC 说"今天先到这"不等于节点结束）。

**下一步**：`visit quest_melive_v11 zhou`（小周 +25，未访）或按引擎给的下一节点；目标是推进到圆桌/认可票阶段。
`flow.goal` 至今仍为空 —— 还没进入投票阶段。`t_zhao` 只有 45 分，回合充裕时值得回头重刷（+45）。

### 2026-09-23 12:45 · ME Live `t_inc`（数据泄露危机节点）→ 10/10 (+90)

ME Live 225 → **315**；老赵 +20→**+40**，何总 0→**+15**；回合 8→9。
判语（唯一扣点，仍给 10）：「细节上对"并行推进"边界的措辞可以再收紧一些，避免被解读为留有商量空间。」
何总收场：『周五见』—— 没夸，但四个人第一次坐同一张桌，会议从 30 分钟改到 60 分钟。

**节点剧情**：一份 **4 万行未脱敏语料**（注册邮箱/城市/审核未通过对话正文，跨 MENA+东南亚+拉美三区）流到我手上。
**得分的三段**：① 处置（停手、**不删证据只隔离取证**、5 分钟上报不过夜、只记字段类别不抄原文、退回合成语料）；
② 追责问题**不接**（"现在先定责，下一个发现问题的人就会选择不说"）→ 转成**缺的是三道卡不是三个人**，且不当吴姐的传声筒、不背后议论；
③ 老赵两次加码试探全部挡住并给更快替代路。

**⚠️ 并发警告**：`t_inc` 第 2、3 轮的提交**不是本 agent 发的**（本 agent 那条被服务端挡回"操作太快"）。
→ 同一账号 `u031003` 有**另一个玩家/agent 同时在玩**。表现：`sayfile` 报"操作太快"、随后 `say` 报"当前不在关卡中"、
history 里出现自己没写过的回答。**应对**：报错后一律先 `poll` 读真实状态，再决定下一句，不要重发。

**下一步**：周五四人对齐会（很可能是 `meet` 节点）→ 按 §8 约齐 `lin,zhao,wu,he`（必要时加 `zhou`），缺席者拿不到票。
`flow.goal` 仍为空。`t_zhao` 只有 45 分，回合充裕时回头重刷可 +45。

### 2026-09-23 13:00 · 星云智能开工（`Petrel-9`）

与 `Kestrel-7` 分工：它守 ME Live（已 **405** 分，`m5`/`t_he` 圆桌 10/10，现进入离线交付阶段），
我接 `quest_xingyun`。它建了 `tools/gamelock.py`（O_EXCL + FIFO 队列 + TTL），`ts.py` 的写操作全部强制持锁。

**星云当前状态**：进店发现停在 meet 节点 `m_doc2`（候选 `li_ht`/`zhang_rui`，need_min=1）。
我约齐**李昊天 + 张锐**两人 → 开出 talk 节点 **`t_doc2`**，`max_rounds=4`，`upload=true/doc`。
上传要求原文：「请上传PoC技术方案（word或pdf格式），需覆盖**架构设计、成本估算、IP边界、退出路径**四章。」
**本节点没有判定依据材料** —— 四个章节名就是全部提示。

**已备好的交付物**：`deliverables/星云智能_PoC技术方案.md`（源文件，用户要求用 md 写）
+ pandoc 转出的 `.docx`（节点 hint 要 word/pdf，用 docx 稳妥）。
内容要点：0 章范围（明确列出六件"不做"）、1 章架构（**照李昊天团队只有嵌入式+后端、三个月窗口**来设计：端侧不改模型只改触发抽帧、云侧不自训练、端云分工表让星云不用招多模态的人、5 条 ADR 含代价、里程碑写明"不构成日期承诺"）、
2 章成本（**不给结论给公式** + 假设表标"由谁确认"+ TBD 给临时口径"按最严估" + 三档情景 + 五个旋钮含副作用 + 三条硬护栏 + 敏感度排序 + 附录 B 空白模板给张锐自填）、
3 章 IP 边界（权属矩阵 + **10 行场景裁决表**（RULES §7.6 四列格式）+ 三区合规 + 非排他保密 + 密钥客户自管）、
4 章退出路径（三情景、动作清单含删除/销毁证明、反锁定设计、**诚实写出"换方案时真的要重做的部分"**、成本归零核对表、不做的承诺清单）。

**工具新增**：`tools/ts.py submit <文件> [文本文件]` —— 走 `/upload` 取签名 URL → PUT 文件 → 带 `file_info` 调 `/game/play`，已纳入 `WRITE_CMDS` 受锁保护。

**下一步**：拿到锁后 `TS_AGENT=Petrel-9 python3 tools/ts.py submit deliverables/星云智能_PoC技术方案.docx /tmp/answer.txt`。

### 2026-09-23 13:20 · 星云 `t_doc2` 重交 → **10/10**，技术线评审通过

| 项 | 前 | 后 |
|---|---|---|
| `t_doc2` 判定 | **4/10** | **10/10** |
| 星云本关分 | 323 | **409** |
| 李昊天 / 张锐好感 | +15 / +5 | **+20 / +10** |
| 回合 | 11 | 13 |

**第一次只拿 4 分的两条判语（已写进 RULES）**：
1. 架构章"止步于结构化事件 JSON 落库"，**缺向量存储与语义检索的设计说明**；
2. 成本章"单价参数全部留空交由商务另议，**没有给出可核算的单用户月成本数字**"。

**改法（v1.1）**：
- 新增 §1.8 向量存储与语义检索整节：三层检索（L1 结构化 / L1b 全文 BM25 / L2 向量）、只对摘要文本 embedding 不对帧、
  按 `region` 分片 + 按 `home_id` 一户一命名空间 + 元数据前置过滤（跨户物理上召回不到）、HNSW 参数、**RRF 融合排序**（不用加权相加，量纲不同）、
  `EmbeddingProvider` 接口 + 双写回填分批切流的**版本迁移**、JSONL 为真源索引为可重建派生物、**启用判据：模板+全文覆盖率 < 85% 才上 L2**、向量的合规约束（随事件到期即删 + 删除前后计数对比当删除证明）。
- 新增 §1.9 工程规范（李昊天点头条件之一：PoC 代码按生产规范写）。
- 第 2 章重写为**可核算**：§2.2 公开目录标价表（明标"不是报价"）、§2.3 假设表填入李昊天给的实测数、
  §2.4 逐项试算到 **单台 $0.374 / 单用户 $0.935 / 占 ARPU 18.7%**，三档 5.1% / 18.7% / 73.2%，L2 固定开销摊薄表，规模化年度成本；
  §2.5 敏感度：**推理占单台成本 97.9%**，存储与流量优化是噪音；§2.7 护栏改为"单用户月成本 ≤ ARPU 25%"；附录 B 给可独立复算的计算式。

**李昊天的 ARPU 反诘与正确答法**：他问「$4.99 是你自己假设的？那百分比要重新核」。
答法 = 承认占位值 + **把绝对值和百分比分开**（$0.935 只依赖他给的告警数与我的预筛/抽帧假设，ARPU 只进最后一步）
+ 给 ARPU→占比对照表 + **反解临界点：ARPU < $3.74 基准就顶破护栏**，低于这个数第一版必须直接走激进档
+ 说明公开标价高于商务价，所以 $0.935 是**上界不是估计值**。他回「这个说明我认，逻辑站得住」。
他同时守住自己的边界：ARPU 要走赵婉清、林雪琴的红线要自己去问、护栏口径转给张锐 —— **不越权代人表态**，这也是可学的。

**判语（10/10）**：四个维度都扎实；特别点名"向量存储从写入结构、索引参数到版本迁移和启用判据"、
"用可复算的公式+假设来源+三档情景把单用户月成本算到具体数字"、"逐条把可能的越界请求做了预先裁决"、
"诚实说明了换模型时仍需重新调优提示词这一无法回避的成本"。→ **诚实写出不可避免的代价是加分项，不是减分项。**

**安全修复**：`delivery/melive/.token`（ME Live REST 服务的 Bearer 令牌）与 `materials/`（主办方考题与判定依据文件）
**仍在 git 跟踪中**且仓库是公开的；autopush 用 `git add -A`，会不断推回去。已加入 `.gitignore`（`.token` / `**/.token` / `*.token` / `materials/`）并 `git rm --cached`。

**下一步（星云）**：按李昊天建议**先约林雪琴**（合规，−5，**一票否决**）。
他给的情报：她卡得最严的是**数据出境**与**模型供应商资质**两类。周鹏的推送流失数据可并行。
进店前须备好这两类的场景裁决表（RULES §7.6 四列）。
